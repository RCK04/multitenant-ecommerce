import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact",
  description:
    "Get in touch with the Sellix team for support, questions, or partnerships.",
};

const Page = () => {
    return (
        <div>
            Contact
        </div>
    )
}

export default Page;